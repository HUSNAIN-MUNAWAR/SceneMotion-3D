# Artifacts

Every completed run can generate:

- extracted frames
- selected keyframes
- feature visualizations
- match visualizations
- trajectory JSON/TXT
- sparse and dense point clouds
- depth PNG/NPY files
- metrics JSON
- rejected pair warnings
- HTML/PDF reports
- benchmark plots
- artifact bundle ZIP

The artifact bundle includes `README_RUN.md`, which explains the files for reviewers.
