# Monocular Scale Limitations

A single camera cannot infer absolute metric scale from image motion alone. Relative translation direction can be recovered, but meters require additional scale information such as known object size, stereo baseline, IMU, depth sensor, or external reference.
